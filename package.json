{
  "name": "osler-anki-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "Phase 1 userscript capture diagnostics for Osler pages.",
  "scripts": {
    "test": "node --test",
    "lint": "node --check userscript/osler-anki-bridge.user.js && node --check tests/osler-anki-bridge.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
