{
  "name": "osler-anki-bridge",
  "version": "0.4.4",
  "private": true,
  "description": "Captura seletiva de cards da Osler com atalhos 1/2, fila persistente, auditoria e exportação TSV para o AnkiDroid.",
  "scripts": {
    "test": "node --test",
    "lint": "node --check userscript/osler-anki-bridge.user.js && node --check docs/osler-anki-bridge.user.js && node --check tests/osler-anki-bridge.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
